[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serves React app for unknown paths
# Static files in dist/ (from public/) take precedence over this
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
